const UserModel = require('./user/admin.model')
const ArticleModel = require('./article/article.model')
const ColumnModel = require('./column/column.model')

module.exports = {
  UserModel,
  ArticleModel,
  ColumnModel
}
